services:
  - type: web
    name: portfolio-optimizer-api
    runtime: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: GEMINI_API_KEY
        sync: false
      - key: DATABASE_URL
        value: postgresql://postgres.ugdktlbidiehftyucuhv:2DFtxw.2Fbqhu5r@aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres

